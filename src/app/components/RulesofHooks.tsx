//1️⃣ : Always write it inside the component or functions
//2️⃣ : Component name must be PascalCase (first letter should be uppercase);
//3️⃣ : we can directly import or we can directly write it using React.hookName;
//4️⃣ : Don't call Hooks inside loops, conditions, or nested functions,but we can use hooks variable like setName inside of loops, conditions or nested functions

import React from 'react'

const RulesofHooks = () => {
    return (
        <div>
            Hooks of ReactJS
        </div>
    )
}

export default RulesofHooks
  